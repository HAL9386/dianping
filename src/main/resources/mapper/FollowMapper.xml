<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dp.mapper.FollowMapper">
  <select id="selectCommonFollowUserIds" resultType="java.lang.Long">
    select f1.follow_user_id
    from tb_follow as f1
    inner join tb_follow as f2
      on f1.follow_user_id = f2.follow_user_id
    where f1.user_id = #{userId}
      and f2.user_id = #{checkUserId}
  </select>
</mapper>
